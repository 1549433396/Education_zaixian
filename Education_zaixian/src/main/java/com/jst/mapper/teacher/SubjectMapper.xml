<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jst.mapper.teacher.SubjectMapper">
	<select id="listAllSM" resultType="Subject">
		select * from sys_subject
	</select>
	<select id="getIdBySM" parameterType="int" resultType="Subject">
		select * from sys_subject where subject_id=#{value}
	</select>
</mapper>